<svg xmlns="http://www.w3.org/2000/svg" width="1140" height="1020" viewBox="0 0 1140 1020" font-family="'Courier New', monospace">
  <defs>
    <marker id="arr" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
      <path d="M0,0 L0,6 L8,3 z" fill="#888"/>
    </marker>
    <marker id="arr-blue" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
      <path d="M0,0 L0,6 L8,3 z" fill="#61dafb"/>
    </marker>
    <marker id="arr-cyan" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
      <path d="M0,0 L0,6 L8,3 z" fill="#00add8"/>
    </marker>
    <marker id="arr-gold" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
      <path d="M0,0 L0,6 L8,3 z" fill="#f3ba2f"/>
    </marker>
    <marker id="arr-red" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
      <path d="M0,0 L0,6 L8,3 z" fill="#ff5252"/>
    </marker>
    <marker id="arr-green" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
      <path d="M0,0 L0,6 L8,3 z" fill="#4caf50"/>
    </marker>
  </defs>

  <!-- Background -->
  <rect width="1140" height="1020" fill="#0d0d0d"/>

  <!-- ═══════════════════════════════════════════
       TITLE
  ═══════════════════════════════════════════ -->
  <text x="570" y="32" text-anchor="middle" font-size="18" font-weight="bold" fill="#f3ba2f">CryptoPulse — Full System Architecture</text>

  <!-- ═══════════════════════════════════════════
       BROWSER
  ═══════════════════════════════════════════ -->
  <rect x="360" y="48" width="420" height="72" rx="8" fill="#0a1e30" stroke="#61dafb" stroke-width="2"/>
  <text x="570" y="70" text-anchor="middle" font-size="13" font-weight="bold" fill="#61dafb">Browser — React + TypeScript (Vite)</text>
  <text x="570" y="87" text-anchor="middle" font-size="10" fill="#99d9f0">5 ticker inputs · validates via Binance REST API (appends USDT if bare ticker)</text>
  <text x="570" y="102" text-anchor="middle" font-size="10" fill="#99d9f0">Sends comma-separated symbols over WS · renders live colour-coded correlation matrix</text>

  <!-- Browser ↔ Caddy -->
  <line x1="570" y1="120" x2="570" y2="150" stroke="#61dafb" stroke-width="1.5" marker-end="url(#arr-blue)"/>
  <line x1="560" y1="150" x2="560" y2="120" stroke="#61dafb" stroke-width="1.5" marker-end="url(#arr-blue)"/>
  <text x="576" y="138" font-size="10" fill="#61dafb">wss://cryptopulseapp.dev/ws</text>

  <!-- ═══════════════════════════════════════════
       CADDY
  ═══════════════════════════════════════════ -->
  <rect x="360" y="153" width="420" height="52" rx="8" fill="#0a200a" stroke="#4caf50" stroke-width="2"/>
  <text x="570" y="174" text-anchor="middle" font-size="13" font-weight="bold" fill="#4caf50">Caddy Reverse Proxy</text>
  <text x="570" y="193" text-anchor="middle" font-size="10" fill="#a5d6a7">SSL/TLS termination · routes /ws → :8080 · sets X-Forwarded-For header</text>

  <!-- Caddy ↔ Backend -->
  <line x1="570" y1="205" x2="570" y2="232" stroke="#4caf50" stroke-width="1.5" marker-end="url(#arr-green)"/>
  <line x1="560" y1="232" x2="560" y2="205" stroke="#4caf50" stroke-width="1.5" marker-end="url(#arr-green)"/>
  <text x="576" y="221" font-size="10" fill="#4caf50">ws://:8080</text>

  <!-- ═══════════════════════════════════════════
       GO BACKEND OUTER BOX
  ═══════════════════════════════════════════ -->
  <rect x="18" y="237" width="1104" height="748" rx="10" fill="#111" stroke="#00add8" stroke-width="2"/>
  <text x="36" y="258" font-size="11" fill="#00add8" font-weight="bold">Go Backend — :8080</text>

  <!-- ═══════════════════════════════════════════
       ROW A — HTTP HANDLERS
  ═══════════════════════════════════════════ -->

  <!-- wsHandler -->
  <rect x="36" y="266" width="230" height="90" rx="6" fill="#0a1828" stroke="#00add8" stroke-width="1.5"/>
  <text x="151" y="286" text-anchor="middle" font-size="12" font-weight="bold" fill="#00add8">wsHandler</text>
  <text x="151" y="302" text-anchor="middle" font-size="10" fill="#888">hub/wsHandler.go</text>
  <text x="151" y="318" text-anchor="middle" font-size="10" fill="#bbb">Upgrades HTTP → WebSocket</text>
  <text x="151" y="333" text-anchor="middle" font-size="10" fill="#888">Origin: cryptopulseapp.dev only</text>
  <text x="151" y="348" text-anchor="middle" font-size="10" fill="#888">Reads messages → symbolReqs chan</text>

  <!-- /stats handler -->
  <rect x="290" y="266" width="200" height="90" rx="6" fill="#0a1828" stroke="#00add8" stroke-width="1.5"/>
  <text x="390" y="286" text-anchor="middle" font-size="12" font-weight="bold" fill="#00add8">/stats endpoint</text>
  <text x="390" y="302" text-anchor="middle" font-size="10" fill="#888">cmd/server/main.go</text>
  <text x="390" y="318" text-anchor="middle" font-size="10" fill="#bbb">GET only · Bearer token auth</text>
  <text x="390" y="333" text-anchor="middle" font-size="10" fill="#888">Returns DailyCounts as JSON</text>
  <text x="390" y="348" text-anchor="middle" font-size="10" fill="#888">Admin UI reads this (AdminPage.tsx)</text>

  <!-- Tracker -->
  <rect x="514" y="266" width="230" height="90" rx="6" fill="#1a180a" stroke="#f3ba2f" stroke-width="1.5"/>
  <text x="629" y="286" text-anchor="middle" font-size="12" font-weight="bold" fill="#f3ba2f">Tracker</text>
  <text x="629" y="302" text-anchor="middle" font-size="10" fill="#888">pkg/tracker/tracker.go</text>
  <text x="629" y="318" text-anchor="middle" font-size="10" fill="#bbb">IP dedup: seen map["date|ip"]</text>
  <text x="629" y="333" text-anchor="middle" font-size="10" fill="#888">Increments DailyCounts on new IP</text>
  <text x="629" y="348" text-anchor="middle" font-size="10" fill="#888">Persists: write-to-temp + atomic rename</text>

  <!-- Hub -->
  <rect x="768" y="252" width="336" height="132" rx="6" fill="#0a1c2a" stroke="#00add8" stroke-width="2"/>
  <text x="936" y="273" text-anchor="middle" font-size="13" font-weight="bold" fill="#00add8">Hub — Central Coordinator</text>
  <text x="936" y="290" text-anchor="middle" font-size="10" fill="#888">hub/hub.go</text>
  <text x="936" y="308" text-anchor="middle" font-size="10" fill="#ccc">clients: map[*websocket.Conn]*Client</text>
  <text x="936" y="323" text-anchor="middle" font-size="10" fill="#ccc">symbols: map[string]*SymbolAttributes (+ symbolLock)</text>
  <text x="936" y="338" text-anchor="middle" font-size="10" fill="#888">Connect(64) · Disconnect(128) · symbolReqs(64)</text>
  <text x="936" y="353" text-anchor="middle" font-size="10" fill="#888">broadcast chan(256) · bufferPool sync.Pool</text>
  <text x="936" y="368" text-anchor="middle" font-size="10" fill="#888">Run(): priority-selects broadcast, then all others</text>

  <!-- wsHandler → Hub -->
  <line x1="266" y1="295" x2="768" y2="305" stroke="#00add8" stroke-width="1.5" marker-end="url(#arr-cyan)"/>
  <text x="430" y="288" font-size="10" fill="#00add8">Connect / Disconnect / symbolReqs chans</text>

  <!-- wsHandler → Tracker (RecordVisit) -->
  <line x1="266" y1="335" x2="514" y2="335" stroke="#f3ba2f" stroke-width="1" marker-end="url(#arr-gold)"/>
  <text x="340" y="328" font-size="9" fill="#f3ba2f">RecordVisit(ip)</text>

  <!-- Tracker → /stats -->
  <line x1="514" y1="320" x2="490" y2="320" stroke="#888" stroke-width="1" marker-end="url(#arr)"/>

  <!-- ═══════════════════════════════════════════
       ROW B — writePump
  ═══════════════════════════════════════════ -->
  <rect x="768" y="402" width="336" height="68" rx="6" fill="#0a1422" stroke="#555" stroke-width="1.5"/>
  <text x="936" y="422" text-anchor="middle" font-size="11" font-weight="bold" fill="#ccc">writePump goroutine (per client)</text>
  <text x="936" y="438" text-anchor="middle" font-size="10" fill="#888">hub/hub.go · spawned in Hub.AddClient()</text>
  <text x="936" y="453" text-anchor="middle" font-size="10" fill="#888">drains client.Send(30) chan → conn.WriteMessage()</text>
  <text x="936" y="468" text-anchor="middle" font-size="10" fill="#888">on error: signals Disconnect chan</text>

  <!-- Hub → writePump -->
  <line x1="936" y1="384" x2="936" y2="402" stroke="#555" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="940" y="396" font-size="9" fill="#555">spawns on AddClient</text>

  <!-- writePump → back to Caddy/Browser (PCC JSON response) -->
  <path d="M 1104 436 C 1120 436 1120 188 780 188" stroke="#61dafb" stroke-width="1.5" fill="none" stroke-dasharray="6,3" marker-end="url(#arr-blue)"/>
  <text x="1090" y="370" font-size="10" fill="#61dafb" writing-mode="tb">PCC JSON → Browser</text>

  <!-- ═══════════════════════════════════════════
       ROW C — Engine label + boxes
  ═══════════════════════════════════════════ -->
  <text x="36" y="498" font-size="10" fill="#555" font-style="italic">─── Engine Layer ────────────────────────────────────────────────────────────────</text>

  <!-- Synchronizer -->
  <rect x="36" y="508" width="200" height="68" rx="6" fill="#0a1828" stroke="#00add8" stroke-width="1"/>
  <text x="136" y="529" text-anchor="middle" font-size="11" font-weight="bold" fill="#00add8">Synchronizer</text>
  <text x="136" y="545" text-anchor="middle" font-size="10" fill="#888">engine/synchronizer.go</text>
  <text x="136" y="561" text-anchor="middle" font-size="10" fill="#888">launches PriceUpdater + Sampler goroutines</text>
  <text x="136" y="573" text-anchor="middle" font-size="10" fill="#888">slidingWindowSize = 600</text>

  <!-- PriceUpdater -->
  <rect x="36" y="598" width="220" height="88" rx="6" fill="#0a1828" stroke="#00add8" stroke-width="1"/>
  <text x="146" y="619" text-anchor="middle" font-size="11" font-weight="bold" fill="#00add8">PriceUpdater goroutine</text>
  <text x="146" y="635" text-anchor="middle" font-size="10" fill="#888">engine/priceUpdater.go</text>
  <text x="146" y="651" text-anchor="middle" font-size="10" fill="#bbb">reads raw JSON from dataStream chan</text>
  <text x="146" y="667" text-anchor="middle" font-size="10" fill="#bbb">unmarshals CombinedStream envelope</text>
  <text x="146" y="682" text-anchor="middle" font-size="10" fill="#888">mid = (bid + ask) / 2 → LatestPrice</text>

  <!-- Sampler -->
  <rect x="284" y="598" width="220" height="88" rx="6" fill="#0a1828" stroke="#00add8" stroke-width="1"/>
  <text x="394" y="619" text-anchor="middle" font-size="11" font-weight="bold" fill="#00add8">Sampler goroutine</text>
  <text x="394" y="635" text-anchor="middle" font-size="10" fill="#888">engine/sampler.go</text>
  <text x="394" y="651" text-anchor="middle" font-size="10" fill="#bbb">time.NewTicker(100ms)</text>
  <text x="394" y="667" text-anchor="middle" font-size="10" fill="#bbb">locks symbolLock · calls RingBuffer.Add(LatestPrice)</text>
  <text x="394" y="682" text-anchor="middle" font-size="10" fill="#888">GetAll() → sampledData → broadcast chan</text>

  <!-- PCC Calculator -->
  <rect x="534" y="508" width="220" height="178" rx="6" fill="#0a1828" stroke="#00add8" stroke-width="1.5"/>
  <text x="644" y="529" text-anchor="middle" font-size="11" font-weight="bold" fill="#00add8">PCC Calculator</text>
  <text x="644" y="545" text-anchor="middle" font-size="10" fill="#888">engine/pcc.go</text>
  <text x="644" y="565" text-anchor="middle" font-size="10" fill="#bbb">Welford's 1-pass online algorithm:</text>
  <text x="644" y="581" text-anchor="middle" font-size="10" fill="#888">meanX += (x[i] - meanX) / (i+1)</text>
  <text x="644" y="596" text-anchor="middle" font-size="10" fill="#888">sumProd += (x[i]-oldMeanX)*(y[i]-meanY)</text>
  <text x="644" y="611" text-anchor="middle" font-size="10" fill="#888">diffSqX += (x[i]-meanX)*(x[i]-oldMeanX)</text>
  <text x="644" y="627" text-anchor="middle" font-size="10" fill="#bbb">r = sumProd / √(diffSqX · diffSqY)</text>
  <text x="644" y="645" text-anchor="middle" font-size="10" fill="#888">NaN guard → returns 0.0</text>
  <text x="644" y="663" text-anchor="middle" font-size="10" fill="#bbb">CalculatePCCMatrix(): all symbol pairs</text>
  <text x="644" y="679" text-anchor="middle" font-size="10" fill="#888">called inside Hub.SendToAll()</text>
  <text x="644" y="678" text-anchor="middle" font-size="10" fill="#888"> </text>

  <!-- ═══════════════════════════════════════════
       ROW D — Data layer
  ═══════════════════════════════════════════ -->

  <!-- SymbolAttributes -->
  <rect x="36" y="710" width="466" height="68" rx="6" fill="#1a180a" stroke="#f3ba2f" stroke-width="1.5"/>
  <text x="269" y="731" text-anchor="middle" font-size="11" font-weight="bold" fill="#f3ba2f">SymbolAttributes (one per active symbol)</text>
  <text x="269" y="747" text-anchor="middle" font-size="10" fill="#888">pkg/models/types.go</text>
  <text x="269" y="763" text-anchor="middle" font-size="10" fill="#bbb">LatestPrice float64 · SlidingWindow *RingBuffer(600) · ClientCounter int · Close chan bool</text>

  <!-- RingBuffer -->
  <rect x="36" y="800" width="466" height="68" rx="6" fill="#1a180a" stroke="#f3ba2f" stroke-width="1.5"/>
  <text x="269" y="821" text-anchor="middle" font-size="11" font-weight="bold" fill="#f3ba2f">RingBuffer — 600 slots (~60 s at 100 ms)</text>
  <text x="269" y="837" text-anchor="middle" font-size="10" fill="#888">pkg/utils/ringBuffer.go</text>
  <text x="269" y="853" text-anchor="middle" font-size="10" fill="#bbb">circular array · Add() overwrites oldest slot · GetAll() returns chronologically ordered copy</text>

  <!-- SymbolAttributes → RingBuffer -->
  <line x1="269" y1="778" x2="269" y2="800" stroke="#f3ba2f" stroke-width="1.5" marker-end="url(#arr-gold)"/>
  <text x="274" y="792" font-size="9" fill="#f3ba2f">contains</text>

  <!-- ═══════════════════════════════════════════
       ROW E — Connection Layer
  ═══════════════════════════════════════════ -->
  <text x="534" y="713" font-size="10" fill="#555" font-style="italic">─── Connection Layer ───────────────────────────────────────────────</text>

  <!-- Connector -->
  <rect x="534" y="722" width="220" height="88" rx="6" fill="#0a1828" stroke="#00add8" stroke-width="1"/>
  <text x="644" y="743" text-anchor="middle" font-size="11" font-weight="bold" fill="#00add8">Connector goroutine</text>
  <text x="644" y="759" text-anchor="middle" font-size="10" fill="#888">connection/connector.go</text>
  <text x="644" y="775" text-anchor="middle" font-size="10" fill="#bbb">1 goroutine per symbol</text>
  <text x="644" y="791" text-anchor="middle" font-size="10" fill="#bbb">spawned by Hub.HandleSymbolRequest()</text>
  <text x="644" y="806" text-anchor="middle" font-size="10" fill="#888">killed via Close chan when ClientCounter → 0</text>

  <!-- CircuitBreaker -->
  <rect x="534" y="830" width="220" height="88" rx="6" fill="#200a0a" stroke="#ff5252" stroke-width="1.5"/>
  <text x="644" y="851" text-anchor="middle" font-size="11" font-weight="bold" fill="#ff5252">Circuit Breaker</text>
  <text x="644" y="867" text-anchor="middle" font-size="10" fill="#888">connection/circuitBreaker.go</text>
  <text x="644" y="883" text-anchor="middle" font-size="10" fill="#bbb">Closed → Open: 9 consecutive fails</text>
  <text x="644" y="899" text-anchor="middle" font-size="10" fill="#bbb">Open → HalfOpen: successful dial</text>
  <text x="644" y="914" text-anchor="middle" font-size="10" fill="#bbb">HalfOpen → Closed: 20 successes, 0 fails</text>

  <!-- EndpointConstructor -->
  <rect x="778" y="722" width="240" height="88" rx="6" fill="#0a1828" stroke="#00add8" stroke-width="1"/>
  <text x="898" y="743" text-anchor="middle" font-size="11" font-weight="bold" fill="#00add8">Endpoint Constructor</text>
  <text x="898" y="759" text-anchor="middle" font-size="10" fill="#888">connection/endpointConstructor.go</text>
  <text x="898" y="775" text-anchor="middle" font-size="10" fill="#bbb">reads BINANCE_WS_URL env var</text>
  <text x="898" y="791" text-anchor="middle" font-size="10" fill="#bbb">builds combined stream URL:</text>
  <text x="898" y="806" text-anchor="middle" font-size="10" fill="#888">/stream?streams=X@bookTicker/Y@bookTicker</text>

  <!-- Binance -->
  <rect x="778" y="830" width="240" height="88" rx="8" fill="#1a140a" stroke="#f3ba2f" stroke-width="2"/>
  <text x="898" y="853" text-anchor="middle" font-size="13" font-weight="bold" fill="#f3ba2f">Binance Futures API</text>
  <text x="898" y="871" text-anchor="middle" font-size="10" fill="#888">wss://fstream.binance.com</text>
  <text x="898" y="887" text-anchor="middle" font-size="10" fill="#bbb">Combined bookTicker stream</text>
  <text x="898" y="903" text-anchor="middle" font-size="10" fill="#bbb">pushes: symbol · bestBid · bestAsk · timestamps</text>

  <!-- ═══════════════════════════════════════════
       ARROWS — Internal flow
  ═══════════════════════════════════════════ -->

  <!-- Synchronizer → PriceUpdater -->
  <line x1="136" y1="576" x2="136" y2="598" stroke="#00add8" stroke-width="1" marker-end="url(#arr-cyan)"/>
  <!-- Synchronizer → Sampler -->
  <path d="M 236 542 C 340 542 340 580 284 608" stroke="#00add8" stroke-width="1" fill="none" marker-end="url(#arr-cyan)"/>

  <!-- Hub spawns Synchronizer (via hub.Run → engine.Synchronizer) -->
  <path d="M 800 358 C 700 440 300 470 236 542" stroke="#888" stroke-width="1" fill="none" stroke-dasharray="4,3" marker-end="url(#arr)"/>
  <text x="490" y="462" font-size="9" fill="#888">hub.Run() → engine.Synchronizer()</text>

  <!-- Hub spawns Connector per symbol -->
  <path d="M 800 360 C 760 600 720 680 754 742" stroke="#888" stroke-width="1" fill="none" stroke-dasharray="4,3" marker-end="url(#arr)"/>
  <text x="755" y="590" font-size="9" fill="#888">HandleSymbolRequest()</text>
  <text x="755" y="602" font-size="9" fill="#888">→ go Connector(...)</text>

  <!-- Connector ← EndpointConstructor -->
  <line x1="778" y1="762" x2="754" y2="762" stroke="#00add8" stroke-width="1" marker-end="url(#arr-cyan)"/>
  <text x="755" y="755" font-size="9" fill="#00add8">URL</text>

  <!-- Connector ↔ Binance -->
  <line x1="898" y1="818" x2="898" y2="830" stroke="#f3ba2f" stroke-width="1.5" marker-end="url(#arr-gold)"/>
  <line x1="888" y1="830" x2="888" y2="818" stroke="#f3ba2f" stroke-width="1.5" marker-end="url(#arr-gold)"/>
  <text x="904" y="828" font-size="9" fill="#f3ba2f">wss:// stream</text>

  <!-- Connector reads from Binance via internal goroutine → internalMsgChan -->
  <!-- Connector → dataStream chan → PriceUpdater -->
  <path d="M 534 762 C 300 762 260 762 256 686" stroke="#00add8" stroke-width="1.5" fill="none" marker-end="url(#arr-cyan)"/>
  <text x="300" y="775" font-size="10" fill="#00add8">dataStream chan []byte</text>

  <!-- CircuitBreaker guards Connector -->
  <line x1="644" y1="818" x2="644" y2="810" stroke="#ff5252" stroke-width="1.5" marker-end="url(#arr-red)"/>
  <text x="648" y="827" font-size="9" fill="#ff5252">guards / expBackoff</text>
  <text x="648" y="838" font-size="9" fill="#ff5252">1s→2s→…→60s + jitter</text>

  <!-- PriceUpdater → SymbolAttributes.LatestPrice -->
  <line x1="146" y1="686" x2="170" y2="710" stroke="#f3ba2f" stroke-width="1.5" marker-end="url(#arr-gold)"/>
  <text x="70" y="705" font-size="10" fill="#f3ba2f">updates LatestPrice</text>
  <text x="70" y="717" font-size="10" fill="#f3ba2f">(under symbolLock)</text>

  <!-- Sampler reads LatestPrice + writes RingBuffer -->
  <line x1="394" y1="686" x2="340" y2="710" stroke="#f3ba2f" stroke-width="1.5" marker-end="url(#arr-gold)"/>
  <text x="380" y="704" font-size="10" fill="#f3ba2f">Add(LatestPrice)</text>
  <text x="380" y="716" font-size="10" fill="#f3ba2f">GetAll() → sampledData</text>

  <!-- Sampler → broadcast chan → Hub -->
  <path d="M 504 638 C 660 580 740 460 800 360" stroke="#f3ba2f" stroke-width="2" fill="none" marker-end="url(#arr-gold)"/>
  <text x="640" y="503" font-size="10" fill="#f3ba2f">broadcast chan</text>
  <text x="640" y="516" font-size="10" fill="#f3ba2f">map[string][]float64</text>

  <!-- Hub.SendToAll → PCC → client.Send → writePump -->
  <path d="M 768 350 C 730 440 700 490 754 548" stroke="#00add8" stroke-width="1.5" fill="none" stroke-dasharray="5,3" marker-end="url(#arr-cyan)"/>
  <text x="705" y="446" font-size="10" fill="#00add8">SendToAll()</text>
  <text x="705" y="459" font-size="10" fill="#00add8">→ CalculatePCCMatrix()</text>

  <!-- PCC → client.Send → writePump -->
  <path d="M 754 600 C 760 490 800 450 836 436" stroke="#555" stroke-width="1.5" fill="none" marker-end="url(#arr)"/>
  <text x="775" y="530" font-size="9" fill="#888">JSON → client.Send chan</text>

  <!-- ═══════════════════════════════════════════
       LEGEND
  ═══════════════════════════════════════════ -->
  <rect x="36" y="894" width="480" height="82" rx="6" fill="#111" stroke="#333" stroke-width="1"/>
  <text x="56" y="914" font-size="11" font-weight="bold" fill="#888">Legend</text>
  <line x1="56" y1="930" x2="100" y2="930" stroke="#00add8" stroke-width="2" marker-end="url(#arr-cyan)"/>
  <text x="108" y="934" font-size="10" fill="#bbb">goroutine / function call</text>
  <line x1="56" y1="950" x2="100" y2="950" stroke="#f3ba2f" stroke-width="2" marker-end="url(#arr-gold)"/>
  <text x="108" y="954" font-size="10" fill="#bbb">data write / channel send</text>
  <line x1="56" y1="969" x2="100" y2="969" stroke="#888" stroke-width="1.5" stroke-dasharray="5,3" marker-end="url(#arr)"/>
  <text x="108" y="973" font-size="10" fill="#bbb">ownership / indirect flow</text>
  <line x1="260" y1="930" x2="304" y2="930" stroke="#61dafb" stroke-width="1.5" stroke-dasharray="5,3" marker-end="url(#arr-blue)"/>
  <text x="312" y="934" font-size="10" fill="#bbb">WebSocket to browser</text>
  <line x1="260" y1="950" x2="304" y2="950" stroke="#ff5252" stroke-width="1.5" marker-end="url(#arr-red)"/>
  <text x="312" y="954" font-size="10" fill="#bbb">circuit breaker guard</text>
  <text x="260" y="973" font-size="10" fill="#888">Sampling period: 100ms · Ring buffer: 600 slots ≈ 60s history</text>

</svg>
